server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: serve index.html for any unknown path (no client routing today, but
    # harmless and future-proof).
    location / {
        try_files $uri $uri/ /index.html;
    }
}
